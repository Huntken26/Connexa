  <head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Connexa</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css"
    >
    <link rel="stylesheet" href="css/style.css" >
  </head>

  <body>
    <div class="flex-column justify-space-around" id="mainbanner">
      <header class="display-flex justify-space-between align-center p-2">
        <h1 class="is-info">
          <span role="img" aria-label="message">💬</span>
          <a href="/">Home</a>
        </h1>
        <nav>

          {{#if logged_in}}
            <a href="/profile">profile</a>
            |
            <button class="no-button" id="logout">logout</button>
          {{else}}
            <section class="hero is-info is-medium is-bold">
              <div class="hero-body">
                <div class="container has-text-centered">
                  <h1 class="title">
                    Welcome to Connexa!
                  </h1>
                  <h2 class="subtitle">
                    Connect & learn without the pressure.
                  </h2>
                </div>
              </div>
            </section>

            <div class="loginLink"><a href="/login">Login here</a></div>
            <div class="signupLink"><a href="/signup">Don't have an account? Sign up now!</a></div>
          {{/if}}
        </nav>
      </header>
      <main class="container container-fluid mt-5">
        {{{body}}}

      </main>
      <footer>

      </footer>
    </div>

    {{#if logged_in}}
      <script src="js/logout.js"></script>
    {{/if}}
  </body>

</html>